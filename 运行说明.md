# 运行说明（Day 7）

> 项目：学英语变强 · PvE 爽游（本地单机网页版）
> 本文档记录如何在本机把这个网站跑起来。零依赖、零构建、无登录无后端。

## 一、环境要求

- 一台 Windows / macOS / Linux 电脑
- 一个现代浏览器：Edge 或 Chrome（其他 Chromium 内核浏览器也可以）
- 不需要安装任何依赖包；方式二需要 Python 或 Node.js 任一（本机均已具备）

## 二、启动方式

### 方式一：双击打开（最快）

1. 打开文件夹 `C:\Users\ZRY\Desktop\Vibe-Coding`
2. 双击 `index.html`，浏览器直接打开即可玩

### 方式二：本地服务器（推荐，交作业截图用这个）

作业截图要求地址栏是 `localhost` 开头，必须用这种方式：

```bash
# 在项目文件夹内执行（二选一）
python -m http.server 8000      # 用 Python
npx serve .                     # 或用 Node.js
```

然后浏览器打开 `http://localhost:8000`。

**停止服务器**：在命令行窗口按 `Ctrl + C`。

## 三、怎么玩

| 场景 | 操作 |
|---|---|
| 主页 | 看角色面板（体力/攻击/防御/移速）、装备与饰品栏、分数、称号 |
| 学习 | 主页点「开始学习」→ 选词组 → 四选一答题（5 题）→ 结算领奖励 |
| 战斗 | 结算页点「进入战斗」；← → / A D 移动，↑ / W / 空格 跳跃，J 攻击 |
| 结算 | 战败弹出本局分数/波数/击杀，可「再来一局」或「返回主页」 |

规则：答错零扣除；战败零损失；每学一组 = 体力 +5、手套 +1（攻击）、护腕 +1（防御）、workbuddy挂坠 +1（收藏）；本周累计 50 分解锁「学习王」、150 分解锁「手法王」。

## 四、数据存在哪

- 所有进度存在**浏览器本地存储**（localStorage），刷新、关浏览器都不丢
- 清档方法：浏览器按 F12 → Application（应用）→ Local Storage → 选中站点 → 右键 Clear，或在 Console 里执行 `localStorage.clear()` 后刷新

## 五、常见问题

| 现象 | 原因与解法 |
|---|---|
| 战斗画布全蓝、没有地面和角色 | 浏览器缓存了旧代码：按 `Ctrl + F5` 强制刷新 |
| 键盘按了没反应 | 先用鼠标在页面任意位置点一下，让键盘焦点进入页面 |
| 进度突然「丢了」 | 双击打开（file://）和 localhost 是两个独立存储，互相看不到档；固定用同一种方式打开即可 |
| 端口被占用（方式二报错） | 换一个端口：`python -m http.server 8001`，浏览器访问 `http://localhost:8001` |
| 想从头验证存档 | 答完题、打完一局后按 F5，数据仍在即正常（A10） |

## 六、验收对照

功能是否齐全以 `docs/PRD.md` 第五章 A0–A13 为准；今天的完成情况见当天的收尾核对表。
